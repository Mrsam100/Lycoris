
export interface Project {
  id: string;
  title: string;
  tag: string;
  color: string;
}

export interface Service {
  id: string;
  name: string;
  color: string;
}

export interface SparkResult {
  brandName: string;
  tagline: string;
  primaryColor: string;
  reasoning: string;
}
