# Lycoris Environment Variables

# IMPORTANT SECURITY NOTE:
# API keys should NEVER be exposed in client-side code in production.
# This is only for development. In production, move this to a backend API.

# Google Gemini AI API Key
# Get your API key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Note: Environment variables prefixed with VITE_ are exposed to the client-side code.
# For production, create a backend API endpoint that uses the API key server-side instead.
